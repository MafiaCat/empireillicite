{
  "name": "jeu-idle-drogue",
  "version": "1.0.0",
  "main": "sw.js",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/index.html && cp -r assets css js manifest.json sw.js www/",
    "sync": "npm run build && npx cap sync ios",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^8.1.0",
    "@capacitor/ios": "^8.1.0"
  }
}